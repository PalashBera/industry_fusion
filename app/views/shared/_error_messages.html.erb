<% if resource.errors.any? %>
  <div class="alert alert-danger" id="error_explaination">
    <% resource.errors.full_messages.each.with_index(1) do |msg, index| %>
      <p class="text-white mb-0">
        <%= index %>. <%= msg %>.
      </p>
    <% end %>
  </div>
<% end %>
