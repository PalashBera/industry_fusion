$('#indent_indent_items_attributes_<%= @unique_id %>_make_id').parents('.selectpicker-filter-dropdown').html('<%= j (render "make_dropdown") %>')

$('#indent_indent_items_attributes_<%= @unique_id %>_uom_id').parents('.selectpicker-filter-dropdown').html('<%= j (render "uom_dropdown") %>')

$(".selectpicker").selectpicker({ size: '7' });
$('.selectpicker').selectpicker('refresh');

if (/Android|webOS|iPhone|iPad|iPod|BlackBerry/i.test(navigator.userAgent)) {
  $('.selectpicker').selectpicker('mobile');
}
